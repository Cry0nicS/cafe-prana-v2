name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Keep in sync with `.nvmrc`, which is what nvm and the local toolchain read.
env:
  NODE_VERSION: 24

# Cancel superseded runs on the same branch/PR so a new push doesn't queue behind stale jobs.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Install node
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Lint
        run: npm run lint

  typecheck:
    name: Typecheck
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Install node
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Typecheck
        run: npm run typecheck

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Install node
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Test
        run: npm test

  content:
    name: Content checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Install node
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Check content parity
        run: npm run check:content
      # Blocking on purpose: an event guests cannot book is a defect, and the
      # fix is a row the owner adds in Studio, in the same file as the hours.
      - name: Check upcoming events are bookable
        run: npm run check:events

  build:
    name: Build application
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Install node
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Build application
        run: npm run build
      # After the build, because it reads the content dump the build writes.
      # Catches a schema or markdown-config change that would lock the owner
      # out of a page in Studio with "Conflict detected" - which neither the
      # build nor the running site notices.
      - name: Check Studio parity
        run: npm run check:studio

  # Single stable check to point branch protection at: green only when every job above passed.
  # `always()` is what makes it report at all when a dependency fails - a skipped gate reports
  # nothing, and the protected branch then waits forever for a status that never arrives.
  # A `skipped` dependency is deliberately not treated as a failure, matching the jobs above,
  # none of which are conditional.
  ci-complete:
    name: CI complete
    if: always()
    needs: [lint, typecheck, test, content, build]
    runs-on: ubuntu-latest
    steps:
      - name: Verify all jobs succeeded
        if: ${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') }}
        run: exit 1
